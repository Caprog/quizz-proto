const WS_PORT = 3000
const WS_URL = `ws://localhost:${WS_PORT}`

const SCOPES = {
    BROADCAST: 'broadcast'
}

const CONTEXTS = {
    HOME: 'home',
    GAME: 'game'
}

export { WS_PORT, WS_URL, SCOPES, CONTEXTS }