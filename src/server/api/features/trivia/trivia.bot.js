

export class TriviaBot {

    selectedAnswer = null

    constructor() {
        
    }

    onMessage({ type, payload }, { send, close }) {
        if (type === 'sync') {
            
        }
    }
}
  